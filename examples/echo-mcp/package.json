{
  "name": "echo-mcp-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal Cloudflare Worker that exposes a 2-tool MCP server using the pokefinder-webmcp library.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
